<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 溢出菜单样式 -->
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:popupBackground">#ffffff</item>
        <item name="android:dropDownVerticalOffset">28dip</item>
        <item name="android:dropDownHorizontalOffset">0dip</item>
    </style>
	<!-- 溢出菜单图标-->
    <style name="OverFlow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/actionoverflowbutton</item>
    </style>

    <style name="MaterialAppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Main theme colors -->
        <!-- your app branding color for the app bar -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 背景颜色 -->
        <item name="android:windowBackground">@color/white</item>
        <!-- 字体颜色 -->
        <item name="android:textColor">@color/black</item>
        <!-- 文本字体大小 -->
        <item name="android:textSize">18sp</item>
        <!-- activity 切换动画 -->
        <item name="android:windowAnimationStyle">@style/activityanimation</item>
        <!-- 溢出菜单样式 -->
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <!-- 溢出菜单图标-->
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
    </style>

    <style name="MaterialAppTheme.NoActionBar" parent="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Main theme colors -->
        <!-- your app branding color for the app bar -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 背景颜色 -->
        <item name="android:windowBackground">@color/white</item>
        <!-- 字体颜色 -->
        <item name="android:textColor">@color/black</item>
        <!-- 文本字体大小 -->
        <item name="android:textSize">18sp</item>
        <!-- activity 切换动画 -->
        <item name="android:windowAnimationStyle">@style/activityanimation</item>
        <!-- 溢出菜单样式 -->
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <!-- 溢出菜单图标-->
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- viewpage 导航标签样式 -->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorPrimary</item>
        <item name="tabIndicatorHeight">3dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/colorPrimary</item>
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="textAllCaps">false</item>
    </style>
    <!-- 登录界面样式 -->
    <style name="Loginactivitystyle" parent="@style/MaterialAppTheme">

        <!-- 无actionbar -->
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- activity切换动画 -->
    <style name="activityanimation">
        <item name="android:activityOpenEnterAnimation">@anim/in_from_right</item>
        <item name="android:activityOpenExitAnimation">@anim/out_from_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/in_from_left</item>
        <item name="android:activityCloseExitAnimation">@anim/out_from_right</item>
    </style>

</resources>